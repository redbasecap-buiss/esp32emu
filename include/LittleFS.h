#pragma once
// esp32emu — LittleFS convenience header
// LittleFS is already defined in SPIFFS.h as an instance of esp32emu::FileSystem.

#include "SPIFFS.h"

// LittleFS object is already available as a global via SPIFFS.h
